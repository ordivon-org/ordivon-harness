# 137 — TYPED FRONTIER TOURNAMENT v2

**Control continuity:** `task:harness-next-conversation-handoff-20260819`  
**Starting authority:** current local `main` at `a0d64337f6bae73e092e13ed5e7d8b76979ff934`.  
**Foundation effect:** none. HaF0–HaF61 remain frozen; HaF62 remains UNKNOWN / NOT SELECTED / NOT ADMITTED.

## 1. Decision question

After Claim Standing Minimal Materialization v1, E5-v2 and Campaign-4 Counterevidence/Rebuttal Direct Dogfood v1 are closed, which remaining frontier now has the strongest **direct empirical / architecture-falsification value without first inventing the relation being tested**?

Candidates:

1. Campaign-4 causal/common-cause dependency / independence;
2. Campaign-3 real rich-effect owner dogfood: partial, delayed, compensating, interfering effects;
3. Campaign-5 provider / locus / internalization OPUR;
4. Agent→Agent delegation / revocation;
5. federation partition / branch / reconciliation;
6. cross-implementation invariance;
7. any newly exposed real engineering / empirical pressure.

No scalar score is used. Dimensions remain separate:

- direct expressibility;
- destructive testability;
- information gain;
- architecture-falsification power;
- owner-boundary cleanliness;
- whether the fixture must invent semantics;
- whether a real production/external-owner surface already exists;
- engineering leverage.

## 2. Revalidated current state

Repository truth was revalidated before the tournament:

- `/root/projects/ordivon-harness` local `main` = `a0d64337f6bae73e092e13ed5e7d8b76979ff934`;
- source worktree is clean;
- `origin/main = cbd606ae655522fac10798889086d9bbcfa8c193`;
- local `main` is ahead 55 / behind 0;
- no history reconstruction/rebase/cherry-pick is authorized for the replication gap.

Current full baseline on the exact starting revision:

- `437` tests run;
- `OK`;
- `3` skipped;
- Runtime Job `job-01a01647-a8c3-7950-ae78-3bce19e47ab1`.

Therefore no current baseline production failure outranks the frontier tournament.

Current Claim Standing production remains:

- `OperationalClaimRef`;
- `OperationalClaimEvidenceRole`;
- `OperationalClaimStandingView`;
- `project_operational_claim_standing_view`;
- no global Claim registry/database/mutable Q.status.

Campaign-4 rebuttal/counterevidence has bounded direct support. Dependency/independence, independent corroboration, arbitrary support-cycle handling, cross-owner support composition and cross-implementation reproduction remain open.

## 3. New empirical readiness probe — Runtime rich-effect surface

A separate disposable Runtime Workspace was opened from the same exact Harness `main`:

`harness-rich-effect-readiness-20260819`

This probe is **not Campaign-3 dogfood** and does not prove Campaign-3 theory. Its only purpose is to determine whether the previously missing rich-effect cases can now be generated by owner-native mechanics rather than fixture-authored semantics.

### 3.1 Partial physical effect is owner-native

Runtime `workspace.execPlan` executed two ordered steps:

1. step 1 wrote `tmp_frontier_partial.txt`;
2. step 2 exited non-zero (`7`).

Runtime result:

- Job `job-01a01649-9a8e-7073-947d-3413b96e5ed7`;
- overall attempt `failed`;
- `completedSteps = 1`;
- `failedStepId = effect-2`;
- the step-1 file remained physically present with exact content `step1\n`.

Therefore a failed composite execution can have a real realized prefix without any fixture declaring a synthetic `PARTIAL` status.

### 3.2 Delayed effect is owner-native

Runtime Job `job-01a01649-f25a-7963-8cbc-87759dbf097b` was admitted with a five-second delayed file write.

Observed sequence:

1. Job projected `working / in_progress`;
2. `tmp_frontier_delayed_v2.txt` was not yet present (`WORKSPACE_PATH_NOT_FOUND`);
3. the same Job later became terminal `succeeded`;
4. the file then existed with exact content `arrived-v2\n`.

Therefore evidence availability and physical realization can be temporally separated by owner-observable state transitions without inventing a delayed-effect relation in the fixture.

### 3.3 Interference / conflict is owner-native

A file began at exact digest for `A\n`.

- Patch `harness-rich-effect-cas-a-to-b-20260819-v1` committed `A -> B`.
- A second patch bound to the same stale pre-change digest attempted `A -> C`.
- Runtime rejected the second operation with `REVISION_MISMATCH`, `commitState = not_committed`.

This provides a real CAS conflict/interference surface: two operations compete over one exact prior state and the owner fails stale continuation closed.

### 3.4 Compensation without historical erasure is owner-native

After `A -> B` committed, a later exact-digest patch `B -> A` committed.

Current bytes returned to the original digest/content `A\n`, while `workspace.patch.get` for the first `A -> B` request still returned its original receipt with `state = committed`.

Therefore a later restoring operation can reverse current state without erasing the prior realized operation/provenance. This is exactly the empirical shape needed to attack Campaign-3's compensation-not-erasure law without a fixture-owned compensation status.

## 4. Candidate C1 — Campaign-4 dependency / independence

### Direct expressibility

**LOW / BLOCKED.**

Current `src/` contains provenance/admission and Claim Standing, but no first-class generic causal/common-cause dependency/independence relation.

### Destructive testability

**LOW now.** Different evidence names, Agent identities or artifact counts cannot establish independence.

### Information gain / architecture-falsification

**HIGH if a real relation is supplied later.** It could directly falsify OCSS corroboration laws and support-cycle assumptions.

### Owner boundary / fixture-semantics risk

Clean only when dependency criteria come from an owner-grounded causal/statistical/operational relation. Current direct fixture would have to invent the criterion it claims to test.

### Existing real surface

**NO generic first-class relation.**

### Disposition

**DEFER. Do not materialize a dependency relation merely so the test can exist.**

## 5. Candidate C2 — Campaign-3 real rich-effect owner dogfood

### Direct expressibility

**HIGH enough for bounded direct dogfood.**

Campaign 3 deliberately rejected one universal scalar Effect Status. Current Claim Standing can carry owner-grounded bounded claims, supporting/counter/required-unknown evidence, subject/use locality and immutable later views. Runtime owns the physical facts.

The readiness probe shows that partial/delayed/interfering/compensating trajectories can be generated as real owner events and state transitions, not as fixture labels.

### Destructive testability

**VERY HIGH.** Prebound cases can attack, among others:

- whole-operation failure must not erase a realized prefix;
- `in_progress`/absence cannot be projected as terminal non-realization;
- stale competing effects must not both be silently admitted;
- later restoring/compensating action must not erase prior realization provenance;
- current state equality after compensation must not imply identical operational history;
- Claim Standing must be able to represent mixed evidence without global mutable Effect status.

### Information gain

**VERY HIGH.** This closes one of the oldest explicitly conceptual-only Campaign-3 gaps with genuinely external-owner evidence.

### Architecture-falsification power

**VERY HIGH.** A direct branch can falsify Claim Standing sufficiency, Runtime-evidence consumption, reconciliation/history preservation or the Campaign-3 claim decomposition itself.

### Owner-boundary cleanliness

**STRONG.** Runtime remains physical execution/effect truth owner. Harness consumes exact owner evidence and mediates operational claim standing. No Harness inference of world/domain semantic success is required.

### Fixture-semantics risk

**LOW if the branch is written against exact Runtime mechanics.** The branch must describe observed transitions and claims; it must not add a synthetic `EffectStatus.PARTIAL/COMPENSATED/INTERFERING` enum just to make cases pass.

### Existing real surface

**YES.** Current Runtime plus existing Harness Runtime/repository-repair bridges already provide the required owner-native mechanics.

### Engineering leverage

**HIGH.** Results directly constrain future Tool-program, multi-step execution, recovery, effect reconciliation and claim-standing consumption without requiring a new subsystem.

### Disposition

**READY — strongest current empirical branch.**

## 6. Candidate C3 — Campaign-5 provider / locus / internalization OPUR

### Direct expressibility

**PARTIAL.** Current production has meaningful `HarnessProviderRoute`, `HarnessProviderUsePolicy`, `HarnessExecutionProfile`, execution binding and provider/morphology evidence.

But the general `Preserves_U(B0 -> B1)` relation is not first-class product semantics.

### Destructive testability

**MEDIUM for bounded provider-route changes; LOW/MEDIUM for general OPUR.** Real locus migration additionally requires Network-owned reachability/service evidence.

### Information gain / architecture-falsification

**HIGH**, especially for provider replacement, cut movement and authority/readmission transitions.

### Owner boundary / fixture-semantics risk

Provider route facts are clean; a general preservation verdict today would still require the experiment itself to author much of `U` and the preservation relation. Locus truth must remain Network-owned.

### Existing real surface

**PARTIAL.** Provider/profile route surfaces exist; general OPUR and real cross-locus transition relation do not.

### Engineering leverage

**HIGH later**, but less direct than Campaign-3 at this exact frontier.

### Disposition

**DEFER until a bounded external use contract makes preservation directly decidable without fixture-owned general OPUR semantics.**

## 7. Candidate C4 — Agent→Agent delegation / revocation

### Direct expressibility

**BLOCKED.** `HarnessExecutionMandate` is caller→Run delegation, not Agent→Agent delegation.

### Destructive testability

Would be high if a real delegation lifecycle existed; direct testing now would manufacture the edge and revocation semantics.

### Existing real surface

**NO first-class Agent→Agent relation.**

### Disposition

**DEFER.**

## 8. Candidate C5 — federation partition / branch / reconciliation

### Direct expressibility

**BLOCKED.** Current same-Run WorkingSet successors and recovery do not instantiate independently bounded federation branches.

### Destructive testability

Potentially very high, but a current fixture would own branch topology/reconciliation semantics.

### Existing real surface

Bounded two-subject federation evidence exists for local identity/adoption/invocation non-deduplication, but no first-class partition→branch→reconciliation relation exists.

### Disposition

**DEFER as direct federation branch evidence.**

## 9. Candidate C6 — cross-implementation invariance

### Direct expressibility

Criteria remain technology-neutral, and historical Codex/Hermes/provider-faithful differences plus DSH comparison provide useful diversity pressure.

### Destructive testability

**LIMITED as a standalone branch.** Distinct adapters/modules inside Ordivon are not automatically independently designed Harness implementations.

### Information gain

**HIGH** for universality claims.

### Fixture-semantics risk

High if local implementation splits are mislabeled as independence.

### Existing real surface

**INSUFFICIENT for strong cross-implementation invariance.**

### Disposition

**STANDING FALSIFICATION PROGRAMME; not selected as the next standalone branch.**

## 10. Candidate C7 — newly exposed engineering / empirical pressure

The current baseline produced no new test failure.

The newly exposed pressure is instead a **readiness-topology change**:

> Runtime's current multi-step Job and digest-fenced Workspace semantics already generate the exact physical shapes that Campaign 3 previously lacked as mature owner evidence.

This is not a new Foundation or a reason to add a generic Effect engine. It is direct empirical pressure on whether current Harness claim/evidence/reconciliation semantics are sufficient when physical reality is richer than one terminal operation outcome.

The especially important new pressure is:

`Same final bytes != same operational history`.

The compensation probe returned current bytes to their original digest while preserving the earlier committed `A -> B` receipt. Any Harness abstraction that collapses those histories solely because endpoints match would be falsified.

## 11. Typed tournament result

No scalar ranking is used. The decisive distinction is **readiness without semantic fabrication**.

| Candidate | Directly expressible now? | Real owner surface now? | Fixture must invent tested relation? | Destructive readiness | Disposition |
|---|---|---|---|---|---|
| C1 Campaign-4 dependency/independence | No | No generic relation | Yes | Blocked | Defer |
| C2 Campaign-3 rich effects | Yes, bounded claim-relative form | Yes — Runtime | No, if exact owner mechanics are used | Very high | **Select empirical branch** |
| C3 Campaign-5 OPUR | Partial | Partial | For general Preserves_U, largely yes | Medium | Defer |
| C4 Agent→Agent delegation | No | No | Yes | Blocked | Defer |
| C5 federation branch/reconciliation | No | No first-class relation | Yes | Blocked | Defer |
| C6 cross-implementation invariance | Criterion yes | Insufficient independent family | Risk of fake independence | Limited | Standing programme |
| C7 new pressure | Yes | Yes | No | High as pressure | Fold into C2 prebinding |

## 12. Selected next branch

**SELECTED NEXT EMPIRICAL BRANCH:**

# Campaign-3 Real Rich-Effect Owner Dogfood v1

This is a post-closeout direct-evidence branch. It does **not** reopen Campaign 3 theory by default and does not create Campaign 7.

The branch must prebind at least four owner-native cases:

1. partial multi-step execution with realized prefix + later failure;
2. delayed effect with an observable nonterminal interval before realization evidence;
3. interfering competing effect attempts with exact stale/current conflict evidence;
4. compensating/restoring later operation where current state returns but prior realization history remains committed.

The direct branch should preferentially consume current public Harness/Runtime bridge surfaces and Claim Standing. If those surfaces cannot express a prebound case without adding a new generic Effect/graph/registry relation, stop and classify an implementation/materialization gap rather than silently expanding production.

## 13. Explicit non-selections

Still not selected:

- Campaign 7;
- HaF62;
- OperationalClaimUseDisposition implementation;
- OCSS dependency graph implementation;
- Agent→Agent delegation implementation;
- federation branch implementation;
- generic Effect Status engine;
- global Claim registry/database;
- cross-implementation universality claim.

## 14. Current standing

- `SelectedHarnessEmpiricalBranch = Campaign-3 Real Rich-Effect Owner Dogfood v1`.
- `Campaign3TheoryStanding = CLOSED / historical closeout preserved`.
- `Campaign4RebuttalStanding = BOUNDED DIRECT SUPPORT`.
- `NextHarnessResearchCampaign = UNKNOWN`.
- `NextHarnessFoundationRoute = UNKNOWN`.
- HaF0–HaF61 remain frozen.
- HaF62 remains UNKNOWN / NOT SELECTED / NOT ADMITTED.
