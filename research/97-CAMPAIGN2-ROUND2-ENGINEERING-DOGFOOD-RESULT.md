# 97 — CAMPAIGN 2 ROUND 2
# Engineering Dogfood Result

**Prebound contract:** `96-CAMPAIGN2-ROUND2-ENGINEERING-DOGFOOD-CONTRACT.md` at commit `ff3fb05` before execution.  
**Runtime Job:** `job-01a01585-6dcf-79f2-b1ad-43c64f51015a`.  
**Mechanical result:** repository-canonical `uv run`; 7 selected tests; 7/7 PASS in 7.186s.

## 1. Round-level classification

`ENGINEERING_SUPPORT_IN_SCOPE`.

The selected fixtures expose the technology-neutral distinctions prebound by P2' across several current mechanisms without code changes. They do not prove arbitrary domain sufficiency or universal cross-implementation equivalence.

## 2. D1 — unbound overlay rejected before Provider dispatch

Mechanical result: PASS.

Implementation fact:

- an arbitrary appended model-visible overlay without completed observation provenance is rejected before Provider dispatch;
- no Provider request and no Runtime execution occurs.

Technology-neutral interpretation:

> Representability != valid Context admission.

Apparently useful bytes cannot become effective Context without the required provenance/authority relation.

P2' pressure: `SUPPORT` for projection validity preceding sufficiency and against Context-superset monotonicity.

## 3. D2 — materialized but unselected source never becomes later cognition

Mechanical result: PASS.

Implementation fact:

- source bytes are durably materialized and remain physically retrievable;
- the current selected Context does not include them;
- after reopen/resume the unselected material still does not appear in model-visible cognition.

Technology-neutral interpretation:

> Availability/materialization != current Context authority.

P2' pressure: strong `SUPPORT` for `Durable/available material != Effective Decision Context` and for explicit selection standing.

## 4. D3 — transient knowledge survives pause then expires at successor

Mechanical result: PASS.

Implementation fact:

- Tool-derived evidence is visible within the current attempt;
- a clean process pause/resume can reconstruct that current-attempt transient exchange;
- after an explicit successor WorkingSet attempt with the same durable source and no promotion, predecessor transient Tool evidence disappears.

Technology-neutral interpretation:

> Context standing is horizon-relative.

The same information is admissibly effective across one horizon and unavailable across a later successor horizon without durable selection/promotion.

P2' pressure: strong `SUPPORT` for P2'-H and against R6 Lifetime Blindness.

## 5. D4 — exact caller subset promotion controls next-interaction durability

Mechanical result: PASS.

Implementation fact:

- two caller messages are both available in the current interaction;
- the Agent explicitly promotes only one exact message;
- provenance-aware projection avoids duplicate exposure while both caller and durable authority coexist in the current interaction;
- in the next interaction the promoted message survives, the unpromoted message does not, and new caller ingress appears separately.

Technology-neutral interpretation:

- immediate effective Context and future durable Context are distinct;
- exact same bytes can change lifetime/authority role through an admitted promotion relation;
- safe omission/retention across a horizon is not inferred from content alone.

P2' pressure: strong `SUPPORT` for basis/effective-context separation and horizon-relative sufficiency.

## 6. D5 — same selected basis, attempt reset discards transient Tool exchange

Mechanical result: PASS.

Implementation fact:

- the successor WorkingSet keeps the same selected durable pin;
- before transition, the current effective request includes transient Tool evidence;
- after transition to the new attempt, the effective request contains only the same durable source and no predecessor transient exchange.

Technology-neutral interpretation:

> Same durable Context basis != same effective decision Context.

This is direct engineering falsification of R7 WorkingSet Identity as a universal Context-equivalence criterion.

P2' pressure: very strong `SUPPORT` for P2'-B.

## 7. D6 — caller ingress changes effective frontier without prior durable-selection change

Mechanical result: PASS.

Implementation fact:

- after an observation-only control gate closes, a fresh resume with no new caller material keeps the gate closed;
- when new caller evidence becomes model-visible on resume, the effective Tool/action surface reopens under the bounded progress rule;
- this occurs through admitted caller ingress rather than a prior WorkingSet selection mutation.

Technology-neutral interpretation:

- effective decision Context can change through a transient admitted ingress while durable selected basis remains unchanged;
- operational control consequences can therefore depend on effective Context rather than durable basis identity alone.

P2' pressure: `SUPPORT` for Effective Decision Context as a first-class analysis layer.

This does not mean the caller evidence is semantically true; only that its admitted exposure changes the bounded operational frontier.

## 8. D7 — forged history reader rejected before Provider dispatch

Mechanical result: PASS.

Implementation fact:

- a forged historical-cognition reader cannot authorize invented recall evidence;
- continuity reconstructs the permitted history from authoritative committed cognition before subsequent Provider dispatch.

Technology-neutral interpretation:

> History availability/claim != current recalled Context authority.

P2' pressure: `SUPPORT` for provenance validity, History != Cognition, and explicit recall/reselection rather than automatic historical injection.

## 9. Rival delta after Round 2

- R7 WorkingSet Identity is now `FALSIFIED_IN_ENGINEERING_SCOPE`, not merely conceptually falsified: D5/D6 show unchanged durable selection with different effective decision Context.
- R6 Lifetime Blindness receives direct engineering falsification through D3/D4/D5.
- R1/R3/R4 receive engineering support against them through D1/D7, though their broader conceptual falsification already stood from Round 1.
- R8 global semantic equivalence remains conceptually falsified; engineering cannot supply missing external relevance authority.

## 10. Cross-implementation standing

P2' is weakened against implementation-dependence but universal invariance remains unproven.

The same higher-level distinctions recur across:

- projection validation;
- source materialization/selection;
- Tool transient cognition;
- caller ingress/promotion;
- attempt boundaries;
- historical recall;
- control-gate recomputation.

These are materially different mechanisms inside one implementation family. This supports a technology-neutral reading but does not replace independent implementation evidence.

## 11. Foundation pressure

`NO_FOUNDATION_PRESSURE` remains.

The dogfood evidence sharpens relations already expressible through existing representation/provenance, retention/history/cognition, ContextFrame and control Foundations. No HaF reopen condition is triggered and no deletion-essential new responsibility appears.

## 12. Next step

Run Round 3 owner-boundary + Campaign-1 compatibility audit, then close Campaign 2 if P2' survives without annexing domain relevance/truth or behavioral equivalence.
