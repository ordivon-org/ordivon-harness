# 143 — POST-PROVIDER-ROUTE TYPED FRONTIER TOURNAMENT v1

**Starting authority:** Campaign-5 Provider-Route Preservation Direct Dogfood v1 closeout at `cd694fc6d672d2e79ccb0f21fcb4d861a062e000`.  
**Control continuity:** `task:harness-next-conversation-handoff-20260819`.  
**Foundation effect:** none. HaF0–HaF61 remain frozen; HaF62 remains UNKNOWN / NOT SELECTED / NOT ADMITTED.

## 1. Decision question

After bounded direct provider-route/use-policy preservation is established, which current Harness frontier has the strongest next information-positive value **without inventing the relation being tested**?

Required candidates:

1. Campaign-4 dependency / common-cause independence / corroboration;
2. Campaign-5 full directional provider transition / live provider replacement;
3. Campaign-5 locus migration;
4. Campaign-5 internalization / externalization;
5. `OperationalClaimUseDisposition` / settlement-consumption materialization;
6. Agent→Agent delegation / revocation;
7. federation partition / branch / reconciliation;
8. cross-implementation invariance;
9. any newly exposed real engineering / empirical pressure.

No scalar score is used. Dimensions remain separate:

- direct expressibility;
- destructive testability;
- information gain;
- architecture-falsification power;
- owner-boundary cleanliness;
- whether the fixture must invent the tested semantics;
- whether a real product/external-owner surface already exists;
- engineering leverage.

## 2. Revalidated starting state

Canonical local Harness main at tournament start:

`cd694fc6d672d2e79ccb0f21fcb4d861a062e000`.

The immediately preceding provider-route branch established bounded direct support under one exact `HarnessProviderUsePolicy`; no production `src/` changes were made by that branch. Its full baseline was `437` tests `OK`, `3` skipped.

Campaign-3 rich-effect direct dogfood and Campaign-4 rebuttal direct dogfood remain closed/current in their bounded scopes. Campaigns 1–6 remain historically closed. Campaign 7 remains unselected.

## 3. Current external-owner locus recheck

Fresh Workstation owner observations were taken during this tournament.

### `surf-clash`

- status = `UNAVAILABLE`;
- namespacePresent = false;
- resolverHealthy = false;
- transportHealthy = false;
- serviceActive = false;
- generation digest remains `sha256:d7e66dcb53f484bb87bcd7649231d5f205b01dafd49a2f4f9e89be910732e92c`.

### `finance-okx`

- status = `UNKNOWN`;
- listenerReachable = false;
- serviceActive = false;
- watchdogDisposition = `no-eligible-member`;
- activeMember = null;
- eligibleMembers = empty.

These are real current owner facts but still do not form an owner-grounded before→after locus transition pair.

`bad/current locus state != locus migration evidence`.

## 4. Claim-use consumer audit

Current production `OperationalClaimRef`, `OperationalClaimEvidenceRole`, `OperationalClaimStandingView` and `project_operational_claim_standing_view` have no production downstream consumer outside the value/projection layer. Current direct uses are tests and research experiments.

Production does contain several real decision/disposition surfaces:

- `NativeRunDisposition`;
- Provider safe-retry/reconciliation state;
- Runtime Tool reconciliation;
- `HarnessToolProgramDurableRecoveryProjection` with `ready-next / terminal / recovery-required`.

But these consume owner/mechanism-specific facts directly. They do not consume Claim Standing.

This creates negative pressure **against** implementing generic `OperationalClaimUseDisposition` by symmetry:

> owner-specific action disposition != generic claim settlement merely because both use words like retry, continuation or reconciliation.

A generic Claim UseDisposition remains justified only when a real consumer must decide a bounded use from one or more exact Claim Standing views and cannot already decide from its authoritative owner-native facts.

No such deletion-essential consumer was found.

## 5. Current provider-transition currentness audit

Historical H5 Codex↔Hermes replacement evidence was rechecked.

Current repository facts:

- current `src/ordivon_harness` contains no Codex/Hermes implementation files;
- `docs/harness-boundary-h5-decision.md` explicitly marks H5 as historical Stage-1 evidence, superseded by extraction/current architecture;
- `docs/VERIFICATION.md` states old Codex/Hermes/replacement receipts are historical and must not certify current code.

Therefore historical H5 transition lineage cannot be combined with current `HarnessProviderUsePolicy` endpoint evidence to manufacture a current direct `Preserves_U(B0 -> B1)` proof.

## 6. Candidate C1 — Campaign-4 dependency / independence

**Direct expressibility:** blocked. Current Claim Standing carries already-classified evidence roles but no generic dependency/common-cause relation.

**Destructive testability:** high once an owner-grounded relation exists, but low now. Differently named evidence, Agents, artifacts or Runtime Jobs cannot by themselves prove causal independence.

**Information gain / architecture falsification:** high in principle.

**Fixture semantic invention risk:** high now. A fixture would have to author the dependency criterion it claims to test.

**Disposition:** `DEFER / RELATION-BLOCKED`.

Do not materialize an OCSS dependency graph solely so the dogfood can exist.

## 7. Candidate C2 — full directional provider transition / live provider replacement

The completed provider-route branch proves endpoint admission under one exact U and use-relative non-global admission.

What remains missing is a current first-class transition relation T carrying transition lineage/currentness/rebinding semantics. Historical H5 is not a current product surface.

A live Provider call would add Provider availability/stochastic evidence but would not by itself create the missing transition relation.

**Disposition:** `DEFER / TRANSITION-RELATION-BLOCKED`.

Do not promote `R0 and R1 are both admitted under U` into `Preserves_U(R0 -> R1)` without current transition evidence.

## 8. Candidate C3 — locus migration

Fresh Workstation observations remain negative-state evidence only. No current owner-grounded before→after migration pair exists.

**Disposition:** `DEFER / OWNER-TRANSITION-EVIDENCE-BLOCKED`.

Do not manufacture a migration by renaming a profile or comparing unrelated unhealthy endpoints.

## 9. Candidate C4 — internalization / externalization

Current Harness has exact Runtime-backed Tool surfaces and application-local composition seams, but no first-class cut-movement/internalization transition relation that would establish one capability moving across the Harness boundary while preserving/readmitting U.

**Disposition:** `DEFER / RELATION-BLOCKED`.

## 10. Candidate C5 — `OperationalClaimUseDisposition`

Current Claim Standing still has no real production downstream consumer.

The existence of `NativeRunDisposition`, safe Provider retry state and ToolProgram recovery disposition does not make generic claim settlement deletion-essential; these are narrower mechanisms with stronger owner-native facts.

Implementing Claim UseDisposition now would risk flattening owner-specific action semantics into a generic layer that no caller requires.

**Disposition:** `DEFER / NO DELETION-ESSENTIAL CONSUMER`.

## 11. Candidate C6 — Agent→Agent delegation / revocation

`HarnessExecutionMandate` remains caller→Run delegation. No first-class Agent→Agent delegation/revocation lifecycle exists.

**Disposition:** `DEFER / RELATION-BLOCKED`.

## 12. Candidate C7 — federation partition / branch / reconciliation

Current WorkingSet successors and Run recovery remain local/single-subject structures. Bounded federation evidence exists for local identity/adoption/non-deduplicated Invocations, but no first-class partition→branch→reconciliation relation exists.

**Disposition:** `DEFER / RELATION-BLOCKED`.

## 13. Candidate C8 — cross-implementation invariance

Current evidence spans many internal mechanisms/adapters, but one Ordivon Harness implementation family remains dominant. Historical external-provider implementations do not constitute independent implementations of current Harness criteria.

**Disposition:** `STANDING FALSIFICATION PROGRAMME / NOT NEXT STANDALONE BRANCH`.

## 14. Candidate C9 — fresh current-product pressure audit

A current-source/active-doc sweep was run specifically to search outside the existing Campaign vocabulary.

It exposed one active architecture statement:

`the separate no-Tool conclusion-correction task remains open`.

This was not accepted at face value. Currentness was reconstructed across Host and Git.

### 14.1 Historical Host truth

`task:harness:no-tool-conclusion-correction-20260810` is Host `completed` revision 3. Historical P6 at canonical `6b4cc7724c83061a03fdb85df0003e9b4bff8d26` causally reproduced and repaired the no-Tool structured-conclusion/control failure, with seven dedicated falsifiers.

This is historical continuity/evidence truth.

### 14.2 Later Git contraction

The current lineage later contains:

- `9087278 harness: preserve provider tool continuation`;
- `9885f3d docs: close provider continuation x2 round`;
- `71bd272 harness: contract unearned continuation machinery`.

The contraction removed `ProviderToolContinuation`, its public/test surface, the dedicated P6 no-Tool falsifier file, and other continuation machinery/evidence. Current `src/` no longer exposes `ProviderToolContinuation`.

Therefore the earlier Host completed task cannot automatically be treated as current product standing after this later semantic contraction.

### 14.3 Direct current-source falsification

The exact historical P6 test file from `6b4cc772...` was executed unchanged against current `cd694fc...` source in an isolated tournament workspace.

Runtime Job:

`job-01a0166f-66e1-7a82-b62c-82dbf244dce2`.

Observed:

- `7` tests executed;
- `6` failed (`4` assertion failures + `2` errors);
- only one historical falsifier still passed.

Current failures included:

1. malformed Harness-native conclusion control now stops `NO_PROGRESS` instead of reaching the corrected `candidate_completed` path;
2. invalid-JSON conclusion control likewise stops `NO_PROGRESS`;
3. multiple unavailable Provider actions stop `INVALID_TOOL_CALL` rather than being one bounded no-Tool correction;
4. repeated malformed conclusion budget semantics differ from the historical accepted correction behavior;
5. unavailable Provider action on a no-Tool surface can again reach `SQLiteHarnessAgentBridge.bind_run_state` with Tool identity/observation state and throw:
   `ToolBridgeError: no-Tool Agent Run cannot bind Tool observations or Tool Call identities`;
6. repeated unavailable Provider turns can reproduce the same strict-bridge failure.

### 14.4 Why this is not merely an obsolete historical expectation

Current production still retains `maxConclusionCorrections` as separate Contract/budget authority.

Current `tests/test_conclusion_correction_separation.py` directly requires:

- conclusion correction with `maxToolCalls = 0`;
- no Tool budget consumption;
- independent `conclusionCorrections` accounting;
- owner/domain rejection feedback followed by a corrected candidate;
- independent conclusion-correction exhaustion semantics.

Therefore current product still claims the **separation of conclusion correction from Tool authority**. The regression is specifically in classification/bookkeeping for malformed Harness-native control and unavailable Provider actions on no-Tool surfaces, not in a deleted promise of generic Provider continuation.

### 14.5 Architecture-falsification power

This pressure is unusually strong because the current official 437-test suite passes while historical exact falsifiers expose the regression. The later contraction removed the regression tests along with unearned continuation machinery, so normal baseline green no longer implies this authority law is covered.

This is a direct example of:

`coverage contraction can reopen a previously closed semantic failure without a baseline failure`.

## 15. Separate active-document currentness defect

The same audit found the reverse kind of drift: active README/STATUS still claim `ProviderToolContinuation` / opaque Provider continuation as a current supported capability even though current source/tests contain no such primitive and Git history explicitly removed it as unearned continuation machinery.

This is a documentation currentness bug, not evidence that the primitive should be restored.

Required repair:

- remove current capability claims for `ProviderToolContinuation` / opaque Provider continuation from active README/STATUS;
- preserve historical Git/evidence provenance;
- do not recreate the deleted primitive.

## 16. Typed comparison

| Candidate | Directly expressible now | Real current surface/evidence | Fixture invents tested semantics? | Destructive power now | Disposition |
|---|---|---|---|---|---|
| C1 Campaign-4 dependency | No | no generic relation | Yes | blocked | Defer |
| C2 directional provider OPUR | endpoint only | U + routes, no T | Yes for T | blocked/medium | Defer |
| C3 locus migration | No transition pair | current unhealthy owner facts only | Yes if forced | blocked | Defer |
| C4 internalization | No | adjacent surfaces only | Yes | blocked | Defer |
| C5 Claim UseDisposition | Type conceptual only | no Claim Standing consumer | Would create unused abstraction | low now | Defer |
| C6 Agent→Agent delegation | No | no relation | Yes | blocked | Defer |
| C7 federation branch | No | no branch relation | Yes | blocked | Defer |
| C8 cross-implementation | criterion yes | insufficient independent family | fake-independence risk | limited | Standing programme |
| **C9 no-Tool control regression** | **Yes** | **current source + exact historical falsifiers + active conclusion-correction contract** | **No** | **very high** | **SELECT** |

No scalar score produced this result. C9 dominates because it is the only candidate with a current executable falsifier against a still-supported authority contract and no need to manufacture semantics.

## 17. Tournament result

**SELECTED NEXT ENGINEERING / EMPIRICAL BRANCH:**

# Current No-Tool Conclusion-Control Regression Reopen & Repair v1

This is **not Campaign 7** and not a new Foundation programme. It is a current product regression branch over an already accepted authority distinction:

`conclusion/control correction != Runtime Tool authority`.

The branch must preserve the contraction decision: do **not** restore `ProviderToolContinuation` or the deleted generic continuation machinery merely because the regression once happened to be fixed on that lineage.

## 18. Required prebound repair scope

Before source modification, prebind at least the historical seven falsifier classes against current product semantics:

1. invalid structured conclusion → conclusion correction, no Runtime Tool history;
2. invalid JSON conclusion control → conclusion correction budget;
3. mixed conclusion + real Runtime Tool intent remains an explicit conflict, not silently swallowed;
4. one unavailable Provider action on a no-Tool surface → model-correctable control/action feedback without Runtime Tool-history authority;
5. multiple unavailable Provider actions → bounded correction without fake Tool identities;
6. repeated unavailable turns are bounded by Tool-correction authority without no-Tool bridge corruption;
7. repeated malformed conclusion control is bounded by conclusion-correction authority.

The exact acceptance contract must also retain current `tests/test_conclusion_correction_separation.py` semantics and full no-Tool/Tool-bearing regression behavior.

## 19. Repair constraints

A valid repair must:

- leave `SQLiteHarnessAgentBridge` strict no-Tool invariant intact;
- classify/correct invalid Harness-native control before it earns Runtime Tool-history authority;
- not grant unavailable Provider actions Runtime Tool authority;
- preserve mixed real Tool intent semantics on Tool-enabled surfaces;
- preserve independent Tool vs conclusion correction budgets;
- not restore `ProviderToolContinuation`;
- not reintroduce deleted continuation/session machinery without a new independent falsifier;
- add focused current regression coverage so a future contraction cannot silently reopen the same defect;
- pass full current baseline after repair.

## 20. Explicit non-selections

Still not selected:

- Campaign 7;
- HaF62;
- generic OCSS dependency graph;
- generic `Preserves_U` implementation;
- locus/internalization implementation;
- `OperationalClaimUseDisposition`;
- Agent→Agent delegation;
- federation branch machinery;
- cross-implementation universality claim;
- restoration of `ProviderToolContinuation`.

## 21. Current standing

- `SelectedHarnessEngineeringBranch = Current No-Tool Conclusion-Control Regression Reopen & Repair v1`.
- `SelectedHarnessEmpiricalBranch = current regression falsification/repair, not a new Campaign`.
- `NextHarnessResearchCampaign = UNKNOWN`.
- `NextHarnessFoundationRoute = UNKNOWN`.
- HaF0–HaF61 remain frozen.
- HaF62 remains UNKNOWN / NOT SELECTED / NOT ADMITTED.


## 22. Additional currentness / release-gate finding before tournament freeze

The tournament documentation check exposed a second pre-existing currentness defect unrelated to the selected no-Tool regression.

`src/ordivon_harness/api.py` already exports the Claim Standing value-layer surfaces admitted by closeout 133:

- `OperationalClaimRef`;
- `OperationalClaimEvidenceRole`;
- `OperationalClaimStandingView`;
- `project_operational_claim_standing_view`.

Research acceptance contract 132 explicitly permitted public exports and U13 required the exact public API exports. Closeout 133 states these are **current product surfaces**.

However `scripts/check_docs.py::STABLE_API` had not been updated, so the repository documentation/release contract failed despite the 437-test Python baseline being green.

This is classified as:

`CLAIM_STANDING_PUBLIC_API_RELEASE_CONTRACT_DRIFT`.

The tournament/currentness branch repairs only the release-tooling expected API set. It does not change `src/`, Claim Standing semantics, or the public API itself.

This reinforces a separate engineering law already visible in the no-Tool regression:

> a green unit-test baseline is not sufficient currentness evidence when dedicated semantic falsifiers or repository acceptance gates have been removed or drifted.
