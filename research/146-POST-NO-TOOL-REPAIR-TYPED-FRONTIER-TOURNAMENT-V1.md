# 146 — POST-NO-TOOL-REPAIR TYPED FRONTIER TOURNAMENT v1

**Starting authority:** current no-Tool conclusion/control repair 145 at canonical `50cb977d6627b29ed3460f577d22c91ca5d7b891`.  
**Control continuity:** `task:harness-next-conversation-handoff-20260819` revision 6 at tournament start.  
**Foundation effect:** none. HaF0–HaF61 remain frozen; HaF62 remains UNKNOWN / NOT SELECTED / NOT ADMITTED.

## 1. Decision question

After current repair 145 restored seven permanent post-contraction falsifiers, which remaining Harness frontier now has the strongest direct information-positive value **without inventing missing semantics**?

Candidates:

1. Campaign-4 dependency / common-cause independence;
2. Campaign-5 full directional provider transition;
3. Campaign-5 locus migration;
4. Campaign-5 internalization / externalization;
5. `OperationalClaimUseDisposition`;
6. Agent→Agent delegation / revocation;
7. federation partition / branch / reconciliation;
8. cross-implementation invariance;
9. semantic regression coverage / contraction-retention gate;
10. any newly exposed current engineering / release pressure.

No scalar score is used. Dimensions remain separate:

- direct expressibility;
- destructive testability;
- information gain;
- architecture-falsification power;
- owner-boundary cleanliness;
- whether a fixture/gate must invent semantics;
- whether a real current surface exists;
- engineering leverage.

## 2. Revalidated starting product

Canonical local main:

`50cb977d6627b29ed3460f577d22c91ca5d7b891`.

Current full suite after repair 145:

- 444 tests;
- OK;
- 3 skipped.

The seven no-Tool conclusion/control falsifiers are now permanent current tests. `SQLiteHarnessAgentBridge` remains strict and unchanged. `ProviderToolContinuation` remains absent.

## 3. Fresh external-owner locus check

Workstation observations remain unchanged in the material sense:

### `surf-clash`

- `UNAVAILABLE`;
- namespace absent;
- resolver/transport/service unhealthy;
- same generation digest `sha256:d7e66dcb53f484bb87bcd7649231d5f205b01dafd49a2f4f9e89be910732e92c`.

### `finance-okx`

- `UNKNOWN`;
- listener unreachable;
- no eligible member;
- no active member.

No owner-grounded before→after locus transition pair exists.

## 4. Previously gated semantic frontiers

### C1 — Campaign-4 dependency / independence

No generic dependency/common-cause relation exists. Differently named evidence, artifacts or Agents cannot establish independence.

**Disposition:** `DEFER / RELATION-BLOCKED`.

### C2 — full directional provider OPUR

Current product has endpoint route/use-policy admission but no current first-class transition T. Historical H5 remains historical.

**Disposition:** `DEFER / TRANSITION-RELATION-BLOCKED`.

### C3 — locus migration

Only current negative owner state exists; no migration pair.

**Disposition:** `DEFER / OWNER-TRANSITION-EVIDENCE-BLOCKED`.

### C4 — internalization / externalization

No first-class cut-movement transition relation exists.

**Disposition:** `DEFER / RELATION-BLOCKED`.

### C5 — `OperationalClaimUseDisposition`

Claim Standing still has no production downstream consumer that must derive action from a standing set. Existing Run/Provider/Tool dispositions consume narrower owner-native facts.

**Disposition:** `DEFER / NO DELETION-ESSENTIAL CONSUMER`.

### C6 — Agent→Agent delegation / revocation

No first-class Agent→Agent lifecycle exists.

**Disposition:** `DEFER / RELATION-BLOCKED`.

### C7 — federation branch / reconciliation

No first-class partition→branch→reconciliation relation exists.

**Disposition:** `DEFER / RELATION-BLOCKED`.

### C8 — cross-implementation invariance

One current Harness implementation family still dominates. Internal adapters/modules are not independent implementations.

**Disposition:** `STANDING FALSIFICATION PROGRAMME / NOT NEXT STANDALONE BRANCH`.

## 5. C9 — semantic regression coverage / contraction-retention gate

Repair 145 exposed a real law:

`Green baseline != preserved semantic coverage when contraction deletes the falsifier.`

This makes a retention gate superficially attractive.

However current post-repair state already changed the relevant causal condition:

- the seven falsifiers are ordinary current tests again;
- full discovery now includes them;
- removing them is a normal source/test deletion visible to Git review;
- Research/Host currentness records preserve why they matter.

A new generic semantic-property registry would require Harness to encode which tests represent which research laws and when a law remains current. That overlaps Research System/owner currentness and risks creating a second mutable authority.

A cheap hard-coded filename/count gate would prevent only one syntactic deletion shape; it could still pass after tests were weakened, renamed with semantic drift, or made vacuous. It therefore has low architecture-falsification value relative to governance cost.

**Disposition:** `DO NOT SELECT GENERIC RETENTION REGISTRY OR TEST-COUNT GATE`.

Reopen only if a second independent contraction incident shows that ordinary permanent regression tests plus existing release/currentness gates are insufficient in a recurring way.

## 6. C10 — current evidence-contract pressure

The tournament then ran the existing release evidence gate:

`python3 scripts/check_evidence.py`.

Runtime Job:

`job-01a01682-5ce0-7711-b078-88af9a60e824`.

It failed for two independent currentness reasons.

### 6.1 Stale verified evidence correctly detected

Entry:

`harness.tool-surface.ts11-turn-working-set`

was still marked `verified` at implementation revision:

`a57963c476d366aea0d73d96cddd223f3bd5dbaf`.

The checker correctly found later invalidating implementation changes in:

- `src/ordivon_harness/api.py`;
- `src/ordivon_harness/claim_standing.py`;
- `src/ordivon_harness/core.py`;
- `src/ordivon_harness/ordivon/loop.py`;
- `src/ordivon_harness/ordivon/sqlite_runtime_bridge.py`.

This is positive evidence that the existing verified→historical currentness mechanism is valuable and should be preserved.

The defect is the stale index status, not the checker rejecting it.

### 6.2 Evidence/file correspondence drift

Four recently admitted immutable evidence files exist under `evidence/` but are absent from `evidence/index.json`:

- `harness-campaign3-rich-effect-owner-v1-capture.json`;
- `harness-campaign3-rich-effect-owner-v1-result.json`;
- `harness-campaign5-provider-route-preservation-v1-result.json`;
- `harness-current-no-tool-conclusion-control-repair-v1.json`.

The existing evidence/file correspondence contract correctly rejects this omission.

### 6.3 Old receipt-shape assumption no longer covers all evidence

`scripts/check_evidence.py` currently assumes every `evidence/*.json` object embeds one of:

- `implementationSourceRevision`;
- `implementationRevision`;
- `sourceRevision`.

The newer research workflow produced typed immutable evidence shapes instead:

- Campaign-3 owner-fact capture embeds `prebindingRevision`;
- Campaign-3 validated result binds back to that capture/closeout but has no old receipt revision field;
- Campaign-5 result is an immutable research result whose prebound tested source is recorded in closeout/Git rather than inside the JSON;
- repair 145 summary embeds `repairCommit` rather than old receipt `implementationRevision`.

Rewriting those frozen evidence bytes merely to satisfy an old checker field convention would break already-published digests/currentness provenance.

Therefore the old universal equation

`evidence JSON == self-identifying old receipt schema`

has been falsified by the current research workflow.

## 7. Existing authority boundary

Active `docs/authority.md` already says:

- `evidence/index.json` classifies repository evidence;
- `scripts/check_evidence.py` validates evidence/file correspondence and revision binding;
- new current claims must identify tests or a receipt bound to the current graph.

Nothing requires revision binding to live inside every immutable evidence payload. The index is already an admitted classification/provenance authority for repository evidence.

Therefore the needed repair is an evolution of the existing evidence contract, not a new semantic registry.

## 8. External revision-binding falsifier

Before selecting a repair, the tournament tested whether external index revision binding can be mechanically grounded without editing evidence bytes.

For each new evidence file, define the tested/bound implementation revision from its already-frozen prebind/repair lineage:

- Campaign-3 capture: `786d64a7cfb21d52e9e541331c3db67a9edd4f29`;
- Campaign-3 result: same `786d64...` validator/prebind implementation;
- Campaign-5 result: `a1a61430047dfa0c43fb2f32d1d2529d57c19018`;
- current no-Tool repair summary: `8925fdba026cdef4f9d8969fae244ee3e5e46730`.

Runtime Job:

`job-01a01684-2dba-7e53-8b99-03ac0ddd90f6`.

For all four files:

1. bound revision is an ancestor of the file's first Git creation commit;
2. the file creation commit is uniquely recoverable from Git history;
3. there are **zero** invalidating implementation-path changes from bound revision through evidence creation.

Creation commits:

- C3 capture/result → `b5cc105a0edddc740c16528a3c68e53e0505e691`;
- C5 result → `cd694fc6d672d2e79ccb0f21fcb4d861a062e000`;
- repair summary → `50cb977d6627b29ed3460f577d22c91ca5d7b891`.

This supplies a bounded mechanically testable external-binding rule:

`bound implementation revision -> unchanged implementation lineage -> immutable evidence creation commit`.

No evidence bytes need rewriting.

## 9. Typed comparison

| Candidate | Directly expressible now | Real current surface | Fixture/gate invents semantics? | Destructive power now | Disposition |
|---|---|---|---|---|---|
| C1 dependency | No | no relation | Yes | blocked | Defer |
| C2 directional provider OPUR | endpoint only | no T | Yes for T | blocked | Defer |
| C3 locus migration | No pair | unhealthy owner state only | Yes if forced | blocked | Defer |
| C4 internalization | No | adjacent only | Yes | blocked | Defer |
| C5 Claim UseDisposition | conceptual | no consumer | unused abstraction | low | Defer |
| C6 Agent delegation | No | no relation | Yes | blocked | Defer |
| C7 federation branch | No | no relation | Yes | blocked | Defer |
| C8 cross-implementation | criterion yes | insufficient family diversity | fake-independence risk | limited | Standing |
| C9 semantic retention registry | tests exist | no recurring independent failure yet | Would create second semantic authority | low/medium | Do not select |
| **C10 evidence currentness contract** | **Yes** | **current release gate already red** | **No new domain semantics** | **high** | **SELECT bounded repair** |

## 10. Tournament result

**SELECTED NEXT ENGINEERING / CURRENTNESS BRANCH:**

# Evidence Index Typed Ingestion & Currentness Repair v1

This is a bounded repair of an existing release/currentness contract.

It is not Campaign 7, not HaF62, not a new research ontology and not a generic semantic regression registry.

## 11. Required repair behavior

The repair must preserve existing old-receipt behavior while adding a narrow second revision-binding mode for immutable evidence projections whose tested revision is carried by the index/Git lineage rather than an old receipt field.

Minimum requirements:

1. existing entries default to embedded revision binding and retain exact current checks;
2. new typed projection entries may explicitly opt into an `index-creation-lineage` binding mode;
3. external binding must prove bound revision is an ancestor of evidence creation commit;
4. external binding must prove no invalidating implementation-path changes between bound revision and evidence creation;
5. if an evidence payload also embeds a standard implementation/source revision, it must not contradict the index;
6. `verified` currentness remains strict against later implementation changes;
7. TS11 stale verified entry is demoted to historical, not re-certified;
8. the four unindexed current evidence files are added without mutating their bytes;
9. the no-Tool repair summary may be `verified` only if its bound repair revision remains current under existing invalidation rules;
10. C3/C5 research projections should remain historical implementation-bound evidence after later source changes, while their scoped research standing remains owned by research closeouts;
11. evidence/file set correspondence remains exact;
12. `scripts/check_evidence.py` must pass after repair.

## 12. Explicit non-selections

Do not add:

- a generic semantic-law registry;
- mandatory test-count or filename retention governance;
- mutable current-claim database;
- evidence-byte rewriting;
- Campaign 7;
- HaF62;
- OCSS dependency graph;
- generic OPUR transition object;
- Claim UseDisposition;
- delegation/federation machinery.

## 13. Current standing

- `SelectedHarnessEngineeringBranch = Evidence Index Typed Ingestion & Currentness Repair v1`.
- `NextHarnessResearchCampaign = UNKNOWN`.
- `NextHarnessFoundationRoute = UNKNOWN`.
- HaF0–HaF61 remain frozen.
- HaF62 remains UNKNOWN / NOT SELECTED / NOT ADMITTED.
