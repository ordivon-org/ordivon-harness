# 126 — POST-DIRECT-DOGFOOD FRONTIER TOURNAMENT v1

**Control task:** `task:harness-post-direct-dogfood-frontier-tournament-20260819`  
**Starting authority:** FOR Direct Dogfood v1 closeout at `2725b70f36d21307ab215040afd2d0d4b87b36f5`.  
**Foundation effect:** none. HaF0–HaF61 remain frozen; HaF62 remains UNKNOWN / NOT SELECTED / NOT ADMITTED.

## 1. Decision question

After first bounded two-subject federation evidence, which next activity has the highest information-positive value?

Candidates:

1. direct Agent→Agent delegation/revocation dogfood;
2. partition → branch → reconciliation federation dogfood;
3. E5 `RealizationClaim / per-subject standing` engineering-consumption materialization;
4. Campaign-3 rich-effect owner dogfood;
5. Campaign-4 accountability independence/rebuttal dogfood;
6. Campaign-5 provider/locus OPUR dogfood;
7. cross-implementation invariance;
8. new Campaign 7 theory.

No scalar score is used.

## 2. Current evidence topology

Current direct federation evidence is BOUNDED:

- shared objective + distinct local Runs;
- local completion scope;
- evidence visibility != adoption;
- explicit cross-subject-origin evidence adoption/provenance;
- distinct subject-local Invocations remain distinct.

One concrete engineering gap was directly exposed:

`ENGINEERING_GAP_SHARED_REALIZATION_CLAIM_SURFACE`.

No direct FOR falsifier was found.

## 3. Candidate C1 — direct delegation/revocation dogfood

Current repository contains strong caller-delegated authority machinery through `HarnessExecutionMandate`, aggregate/attempt budget fencing, exact mandate binding and authority-envelope rejection.

However, current public Harness surface does **not** expose a first-class Agent→Agent delegation relation or revocation lifecycle.

A fixture could let A create metadata and then externally construct B's mandate, but the actual delegation edge/revocation semantics would be fixture-owned. That would pressure local mandate enforcement, not directly dogfood FOR's Agent→Agent delegation relation.

Decision: **DEFER until a selected engineering-consumption relation exists or an external owner supplies a real delegation contract.**

## 4. Candidate C2 — partition → branch → reconciliation dogfood

Current repository contains strong same-Run/local cognition successor machinery:

- WorkingSet successors;
- competing transition fail-closed behavior;
- process recovery;
- supersession/history preservation.

These are useful adjacent evidence but not a first-class federation branch relation between independently bounded AgentOperationalSubjects.

A federation partition fixture today would require fixture-owned branch topology and reconciliation semantics, risking circular confirmation.

Decision: **DEFER as direct federation evidence; retain as adjacent pressure.**

## 5. Candidate C3 — E5 Realization Claim Standing engineering consumption

Campaign 3 already established the technology-neutral relation stack:

- explicit bounded Realization Claim `Q`;
- issuer/owner-grounded claim scope;
- evidence projection validity;
- evidence-relative standing for Q;
- mixed scoped claims rather than universal scalar effect status;
- use-relative settlement/reconciliation;
- continuation/redispatch obligations.

FOR adds the need for:

- one shared Q identity;
- different subject-local admissible evidence views/standings;
- later evidence adoption without truth-owner transfer.

FOR Direct Dogfood v1 then observed that no generic public first-class surface exists to express this directly.

This is a high-quality engineering-consumption problem because:

1. the semantics are already research-grounded;
2. a direct empirical branch exposed the materialization gap;
3. resolving the contract boundary enables later direct falsification instead of theory expansion;
4. the problem can be studied without immediately changing production code;
5. it tests whether a generic Harness surface is actually warranted or owner-specific claim contracts should remain external.

Decision: **SELECTED.**

## 6. Candidate C4 — Campaign-3 rich-effect owner dogfood

High value for partial/delayed/compensating/interfering physical effects. But it requires real owner/external evidence and does not currently resolve the inability to represent shared Q/local standing generically.

Decision: defer behind C3.

## 7. Candidate C5 — Campaign-4 accountability dogfood

Still valuable, especially independent corroboration/rebuttal/common-cause cases. Current OCSS theory and engineering already have a usable materialization surface; the federation claim-standing gap is more immediate.

Decision: defer.

## 8. Candidate C6 — Campaign-5 provider/locus OPUR dogfood

High value but primarily external/provider/locus evidence acquisition. It does not unblock the explicit E5 gap.

Decision: defer.

## 9. Candidate C7 — cross-implementation invariance

Still important, but independent implementation-family diversity remains insufficient.

Decision: standing falsification programme, not next branch.

## 10. Candidate C8 — Campaign 7 theory

No current conceptual contradiction requires another theory campaign. Continuing theory now would outrun the newly exposed engineering-consumption boundary.

Decision: not selected.

## 11. Tournament result

**SELECTED NEXT WORK — ENGINEERING-CONSUMPTION RESEARCH, NOT CAMPAIGN 7:**

# Operational Claim Standing Engineering Consumption v0
## Realization Claim / Evidence Standing / Subject View Minimal Contract

This is not production implementation.

The branch must decide whether and how Campaign-3 + FOR semantics should materialize into a minimal, technology-neutral Harness engineering-consumption contract.

## 12. Required questions

The branch must answer at least:

1. What is the minimal identity of a bounded `OperationalClaimRef` / Realization Claim Q?
2. Which owner/issuer information is required without making Harness the truth owner?
3. Is standing a property of Q, of evidence, or of a subject/use-relative projection over Q and evidence?
4. How are `SUPPORTED / CONTRADICTED / CONFLICTED / UNDERDETERMINED` represented without claiming world truth?
5. How does evidence adoption change one subject's view without rewriting Q or another subject's view?
6. How are claim scope/currentness/version/epoch represented?
7. How does reconciliation/settlement remain use-relative and history-preserving?
8. Which parts belong in Harness versus Runtime/domain/Normative/Network owner contracts?
9. Does a generic claim-standing surface actually improve interoperability, or would it become a shadow truth authority?
10. What is the minimum direct dogfood enabled by the contract if it is admitted?

## 13. Required rivals

At least compare:

- R1 one global mutable `EffectStatus`;
- R2 claim object stores authoritative truth directly;
- R3 standing is globally attached to Q;
- R4 evidence receipt itself is the standing object;
- R5 each Agent copies/forks Q when evidence differs;
- R6 generic Harness claim registry owns all domain claims;
- R7 no generic claim reference at all; all claims remain owner-specific opaque evidence;
- R8 materialize production schema immediately from Campaign-3 prose.

## 14. Stop / admission rule

The branch may end with any of:

- `GENERIC_MINIMAL_CONTRACT_ADMITTED`;
- `OWNER_SPECIFIC_ONLY`;
- `PROJECTION_ONLY_NO_REGISTRY`;
- `MATERIALIZATION_DEFERRED`;
- `THEORY_REOPEN_REQUIRED`.

No production class/schema/table is authorized until this research branch closes and explicitly selects an engineering-consumption form.

## 15. Selection standing

- `SelectedHarnessEngineeringConsumptionBranch = Operational Claim Standing v0`.
- `NextHarnessResearchCampaign = UNKNOWN`.
- `NextHarnessFoundationRoute = UNKNOWN`.
- HaF62 remains UNKNOWN / NOT SELECTED / NOT ADMITTED.
